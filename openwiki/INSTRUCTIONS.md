Build a concise maintainer wiki for this repository. Ground every claim in tracked
source, tests, current contributor guidance, and recent git history. Prioritize the
quickstart, package/runtime boundaries, security and persistence boundaries, terminal
agent workflow, evaluation workflow, generated-file ownership, and exact verification
commands. Link to primary tracked files instead of duplicating long reference material.

Honor every repository-visible-language restriction in AGENTS.md. Never include
credentials, environment values, machine-specific absolute paths, dirty-worktree
contents, or untracked user files. Treat generated outputs, network tools, approval
state, remote sandboxes, MCP servers, and model output as separate trust boundaries.
Keep index files navigational and keep all local links valid.
