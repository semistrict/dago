package loop

// The upstream artifact remains as a contract marker. Its package-private
// legacy tool executor was removed; the migrated cases now exercise Dago's
// compiled tool node through ProcessOneTurn.
