// Package dasandbox resolves and owns remote sandbox sessions across built-in,
// extension, and application-configured provider factories.
//
// The package performs no credential discovery. Applications explicitly bind
// authenticated provider implementations to registry names.
package dasandbox
